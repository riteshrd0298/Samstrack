import { ShowAllAttendance } from './show-all-attendance';

describe('ShowAllAttendance', () => {
  it('should create an instance', () => {
    expect(new ShowAllAttendance()).toBeTruthy();
  });
});
